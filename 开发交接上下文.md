# 实习生全周期智能成长平台：开发交接上下文

> 更新时间：2026-08-03  
> 当前项目：`D:\招商银行ai\实习生全周期智能成长平台\dream-factory-web`

## 1. 项目目标

当前阶段建设一个“青春梦工场”实习生全周期智能成长平台前端原型。现有页面包括：

- 身份选择登录页
- 梦工场首页
- 新手任务页
- 主线任务页
- 专业任务页

当前只开发前端，但代码必须按照前后端分离方式组织。所有任务、统计、进度、奖励、日期、状态、导师信息等业务数据都应由接口数据驱动，不能写进图片，后续接入后端时不应重写页面组件。

## 2. 已确认的视觉与素材要求

### 2.1 顶部统计图标

三个任务页面统一复用以下三个 2.5D 游戏风透明 PNG：

- 本周完成：任务清单与勾选图标
- 已获星愿值：金色星星图标
- 距本周结束：日历图标

头像区域的星愿值显示也复用金色星星素材，只通过 CSS 调整尺寸。

### 2.2 左侧任务分类图标

新手、主线、专业任务各使用一枚透明 PNG：

- 新手任务：带嫩芽的成长手册
- 主线任务：旗帜与任务卷轴
- 专业任务：公文包与职业徽章

图标不包含分类按钮的背景方框。按钮背景、描边、选中态和主题颜色由 CSS 控制：

- 新手任务：绿色
- 主线任务：红色
- 专业任务：紫色

### 2.3 底部导航图标

首页与三个任务页统一复用五个透明 PNG：

- 梦工场
- 任务
- 成长
- 积分
- 我的

普通态、悬停态和选中态由 CSS 控制。任务页面中“任务”入口高亮；首页中“梦工场”入口高亮。所有页面保持相同图标造型。

### 2.4 横向任务卡活动图片

所有横向任务卡左侧统一复用同一张通用活动 PNG。当前素材为任务卷轴、蓝色任务面板和金色星光的组合。

该图片只负责视觉表现，不包含任务名称、进度、人数、奖励、日期或状态。导师头像和用户头像不属于任务卡活动图。

## 3. 最终素材

最终使用的 12 个透明 PNG 位于：

`public/assets/icons/`

文件清单：

```text
activity-shared.png
category-mainline.png
category-newcomer.png
category-professional.png
nav-growth.png
nav-home.png
nav-points.png
nav-profile.png
nav-task.png
stat-calendar.png
stat-complete.png
stat-star.png
```

这些文件均已验证具有 Alpha 通道，页面加载时没有损坏图片。图标资源路径统一集中在 `src/constants/assets.ts`，组件中不要散落硬编码路径。

## 4. 已完成的前端重构

原项目主要逻辑集中在单个 `src/App.tsx` 中，现已拆分为：

```text
src/
├─ components/
│  ├─ BottomNavigation.tsx
│  ├─ MentorCard.tsx
│  ├─ PlayerPanel.tsx
│  ├─ StatsStrip.tsx
│  ├─ TaskRow.tsx
│  └─ TaskSidebar.tsx
├─ constants/
│  ├─ assets.ts
│  └─ navigation.ts
├─ mocks/
│  ├─ authData.ts
│  ├─ homeData.ts
│  └─ taskData.ts
├─ pages/
│  ├─ HomePage.tsx
│  ├─ LoginPage.tsx
│  └─ TaskCenterPage.tsx
├─ services/
│  ├─ authService.ts
│  └─ taskService.ts
├─ types/
│  ├─ auth.ts
│  └─ task.ts
├─ App.tsx
├─ main.tsx
└─ styles.css
```

### 各层职责

- `types/`：前端领域模型、DTO 形状和页面标识。
- `mocks/`：当前演示数据，不应被页面组件直接读取。
- `services/`：页面访问数据的统一边界。目前返回模拟数据，后续在这里替换为 HTTP 请求。
- `constants/`：资源映射、导航顺序等固定配置。
- `components/`：跨页面复用的展示和交互组件。
- `pages/`：页面级数据加载、状态组合和页面布局。
- `App.tsx`：当前基于 URL hash 的轻量页面路由。

## 5. 数据和后端接入约定

当前接口边界位于：

- `src/services/authService.ts`
- `src/services/taskService.ts`

现有服务包括：

- `getTaskPages()`：取得三个任务页面的完整数据。
- `getTaskPage(pageId)`：取得指定任务页面数据，预留给按页请求场景。
- `getUserSummary()`：取得用户等级和星愿值等摘要。
- `getHomeModules()`：取得首页模块入口数据。
- `signInAsRole(role)`：当前根据身份返回模拟用户，后续在这里替换为后端登录接口。
- `restoreSession()` / `saveSession(user)`：管理当前标签页内的模拟登录会话。

目前服务内部使用 `structuredClone` 返回模拟数据，避免组件意外修改数据源。接入后端时，应保留服务函数签名，改为调用真实 API，并根据后端响应增加 DTO 到前端模型的转换。

当前登录页仅提供“我是实习生”和“我是管理员”两个身份入口。模拟会话保存在 `sessionStorage`，只用于前端演示，不能作为正式权限认证。后续接入后端时，应由后端返回用户身份和权限，并在服务层替换模拟认证逻辑。

以下内容必须继续保持数据化：

- 任务名称、说明与栏目
- 完成数量、总数和百分比
- 星愿值与任务奖励
- 剩余天数
- 任务进度、状态和按钮文字
- 活动时间、地点与剩余名额
- 岗位与能力标签
- 导师姓名、部门、指导内容、反馈及沟通安排
- 用户名称、等级和总星愿值

不要为了还原视觉稿而将上述数据重新合并进 PNG。

## 6. 当前页面行为

- 未建立模拟会话时首先显示身份选择登录页。
- 点击“我是实习生”后保存当前标签页会话并进入原梦工场首页，刷新后仍保持当前身份。
- 点击“我是管理员”目前只显示“管理员工作台正在建设中”提示，不进入其他页面，也不保存管理员会话。
- 首页底部“任务”按钮进入 `#/tasks/newcomer`。
- 三个任务分类通过左侧导航切换：
  - `#/tasks/newcomer`
  - `#/tasks/mainline`
  - `#/tasks/professional`
- 底部“梦工场”返回首页。
- 尚未开发的成长、积分、我的等模块暂时显示提示消息。
- 任务操作按钮在点击后变为完成状态，用于演示交互；目前状态只保存在页面内存中。
- 专业任务页包含导师卡片和合规提示。

## 7. 首页实现注意事项

首页主体仍使用：

`public/dream-factory-home.png`

作为互动地图背景。背景图片自身包含旧版底部导航画面，因此当前用统一的真实导航组件覆盖该区域。后续如果重新导出首页背景，建议生成一个不含底部导航的纯地图版本，届时可减少覆盖层并提高响应式适配自由度。

首页各建筑入口目前使用透明热点按钮覆盖在背景图上。修改背景图尺寸或建筑位置时，需要同步检查 `styles.css` 中各个 `.hotspot--*` 的定位。

## 8. 样式与响应式约定

任务页主题通过页面类和 CSS 变量控制：

- `.task-page--newcomer`
- `.task-page--mainline`
- `.task-page--professional`

公共组件不要通过复制 CSS 创建三套版本，应继续使用 `--accent`、`--accent-dark`、`--accent-soft`、`--accent-rgb` 等变量切换主题。

已经检查过：

- 1920 × 1080 桌面布局
- 1280 × 720 较小桌面布局
- 390 × 844 移动端布局

移动端底部导航固定在视口底部，任务内容可纵向滚动。当前没有页面级横向溢出。

## 9. 已完成的验证

在 C 盘工作副本完成开发后，代码和最终素材已经同步到本 D 盘项目，并在 D 盘项目目录重新执行构建。

验证结果：

- TypeScript `tsc -b`：通过
- Vite 生产构建：通过
- 首页五个导航图标：全部加载成功
- 三个任务页顶部图标：全部加载成功
- 三个任务页分类图标：全部加载成功
- 三个任务页任务卡：每页五张卡均引用同一个 `activity-shared.png`
- 首页导航进入任务页：通过
- 任务按钮点击和禁用状态：通过
- 页面应用控制台错误：无
- 损坏图片数量：0

构建产物位于 `dist/`。

## 10. 当前 Git 状态

本次修改尚未提交。主要变更包括：

- 修改 `src/App.tsx`
- 修改 `src/styles.css`
- 新增 `public/assets/icons/`
- 新增 `src/components/`
- 新增 `src/constants/`
- 新增 `src/mocks/`
- 新增 `src/pages/`
- 新增 `src/services/`
- 新增 `src/types/`

后续开发者开始前应先运行：

```powershell
git status --short
```

确认并保留上述未提交修改，不要使用 `git reset --hard` 或覆盖这些文件。

## 11. 推荐的后续工作

1. 确认登录页和四个原有页面的最终视觉细节，包括图标显示尺寸、任务卡密度和底部导航高度。
2. 若有条件，重新输出不含导航栏的首页背景图。
3. 为任务服务增加统一的请求状态、错误状态和重试处理。
4. 确定后端 API 路径与响应 DTO，并在服务层完成映射。
5. 将任务完成状态从组件内存迁移到接口提交和服务端持久化。
6. 根据实际路由规模决定是否引入 React Router。
7. 补充组件测试、服务层测试和关键页面端到端测试。
8. 检查无障碍文本、键盘焦点和不同屏幕尺寸下的可用性。
9. 建设管理员工作台后，将管理员身份入口接入对应路由，并由后端执行真实角色鉴权。

## 12. 工作目录说明

后续开发应直接以以下 D 盘目录作为 Codex 或 IDE 工作区：

`D:\招商银行ai\实习生全周期智能成长平台\dream-factory-web`

此前使用过的 C 盘项目副本只是受权限限制时的中转工作副本。确认 Codex 工作区已经切换到 D 盘后，可以删除 C 盘的 `dream-factory-web` 副本。不要在两个副本中同时开发，以免再次出现同步和版本分叉。
